# AGENTS.md

이 파일은 이 저장소에서 작업하는 모든 개발자와 코딩 에이전트가 따라야 할 공통 지침입니다. 더 하위 디렉터리에 별도의 `AGENTS.md`가 생기면 그 디렉터리에서는 하위 지침을 함께 적용합니다.

## 1. 제품 목표

초등교육과 관련된 AI·디지털 교육 뉴스를 하루 한 편씩 제공하는 자동 큐레이션 웹사이트를 만든다. 서비스는 뉴스를 단순 복제하거나 많이 나열하지 않는다. 여러 신뢰할 수 있는 출처를 바탕으로 핵심 사실과 맥락을 짧게 재구성하여 학생, 교사, 학부모의 생각을 넓히는 것이 목적이다.

우선순위는 다음과 같다.

1. 사실성과 출처 추적 가능성
2. 중단 없이 복구 가능한 일일 자동화
3. 쉽고 차분한 한국어 콘텐츠
4. 단순하고 빠른 갤러리형 읽기 경험
5. 한 명의 운영자가 감당할 수 있는 유지보수성과 비용

## 2. 작업 전 확인 사항

- 작업을 시작하기 전에 `README.md`와 관련 파일을 읽는다.
- 담당 영역에 해당하는 `coder/` 프로필을 확인한다.
- 기존 사용자의 변경사항을 보존하고 관련 없는 파일을 수정하지 않는다.
- 요구가 모호해도 안전하고 되돌릴 수 있는 범위에서는 합리적인 가정으로 진행한다.
- 데이터 손실, 외부 게시, 유료 서비스 사용 증가처럼 영향이 큰 선택은 임의로 확장하지 않는다.

## 3. 역할별 책임

### 백엔드·데이터

기준 프로필: `coder/01_김도윤_백엔드_아키텍트.md`

- RSS와 공식 API를 우선해 뉴스를 수집한다.
- 수집기는 소스별로 분리하고 한 소스의 실패를 격리한다.
- 원문 URL, 매체, 제목, 발행일, 수집 시각을 반드시 보존한다.
- 정규 URL과 콘텐츠 지문을 이용해 기사 중복을 방지한다.
- 모든 쓰기 작업은 재실행 가능한 멱등성을 갖춰야 한다.

### AI·콘텐츠

기준 프로필: `coder/02_박서연_AI_콘텐츠_엔지니어.md`

- 모델에는 선택된 근거 자료만 제공하고 구조화된 결과를 요구한다.
- 출처에서 확인되지 않은 사실을 추가하지 않는다.
- 사실과 해석을 구분하며, 불확실한 내용은 단정하지 않는다.
- 생성 결과는 형식, 출처, 중복, 사실성 검사를 통과해야 한다.
- 모델명, 프롬프트 버전과 사용한 출처를 실행 기록에 남긴다.

### 프런트엔드·제품

기준 프로필: `coder/03_이현우_프런트엔드_엔지니어.md`

- 메인 화면의 중심은 최신순 카드 갤러리다.
- 초기 범위에 불필요한 메뉴, 대시보드, 회원 기능, 댓글을 추가하지 않는다.
- 모바일을 먼저 고려하고 키보드 탐색과 명도 대비를 지킨다.
- 의미 있는 HTML을 사용하며 모든 대표 이미지에 대체 텍스트를 제공한다.
- 공개 화면에는 검증되어 발행된 게시물만 표시한다.

### 플랫폼·운영

기준 프로필: `coder/04_최민재_플랫폼_신뢰성_엔지니어.md`

- 일일 작업의 각 단계와 결과를 구조화된 로그로 기록한다.
- 재시도에는 횟수 제한과 지수 백오프를 적용한다.
- 같은 날짜의 게시물이 두 번 발행되지 않도록 PostgreSQL 고유 제약과 트랜잭션으로 보장한다.
- API 키와 자격 증명을 저장소, 클라이언트 코드, 오류 로그에 노출하지 않는다.
- 발행 누락, 지속적인 수집 실패, 비용 한도 초과를 운영자가 알 수 있게 한다.

## 4. 고정 콘텐츠 규격

게시물은 아래 네 영역을 유지한다.

1. 오늘의 한 줄 요약
2. 무슨 일이 있었나요?
3. 함께 생각해 볼 질문
4. 참고 기사와 출처

변경 요구가 없는 한 별도의 `초등교육과의 관련성`, `학생·교사·학부모에게 미치는 영향` 섹션을 만들지 않는다. 필요한 관점은 본문 안에 자연스럽게 녹인다.

작성 원칙:

- 기본 언어는 한국어다.
- 초등학생도 큰 어려움 없이 이해할 수 있는 문장을 사용한다.
- 자극적인 제목, 클릭 유도 문구, 과도한 느낌표를 사용하지 않는다.
- 특정 기술, 기업, 정책을 근거 없이 홍보하거나 비난하지 않는다.
- 질문은 독자가 여러 관점에서 생각할 수 있게 작성한다.
- 원문을 길게 인용하거나 문장만 바꿔 재게시하지 않는다.
- 각 참고 출처에는 제목, 매체, 발행일, 원문 링크를 표시한다.

## 5. 뉴스 수집 및 선정 규칙

- 수집 전에 소스의 이용 조건과 접근 허용 범위를 확인한다.
- 로그인이 필요하거나 우회 접근이 필요한 콘텐츠는 수집하지 않는다.
- 호출 속도를 제한하고 식별 가능한 사용자 에이전트를 사용한다.
- 광고성 보도자료 한 건만으로 중요한 사실을 확정하지 않는다.
- 선정 점수에는 초등교육 관련성, 신뢰도, 새로움, 사회적 의미를 포함한다.
- 동일 사건을 다룬 기사는 하나의 주제로 묶는다.
- 과거 게시물과 의미상 중복되는 주제는 새 정보가 있을 때만 선정한다.
- 발행 기준을 만족하는 후보가 없으면 사실을 꾸며내지 않는다.

## 6. 자동화 불변 조건

다음 조건은 구현 방식과 관계없이 지켜야 한다.

- 한국 표준시 기준 하루 최대 한 건만 발행한다.
- `PipelineRun`은 실행 날짜와 단계별 상태를 식별할 수 있어야 한다.
- 실패한 실행은 완료된 단계를 훼손하지 않고 재개할 수 있어야 한다.
- 발행은 모든 품질 검사가 완료된 뒤 마지막 단계에서 수행한다.
- 게시물과 사용한 출처의 관계는 발행 후에도 보존한다.
- 외부 API가 실패할 때 미완성 콘텐츠를 공개하지 않는다.
- 작업 제한 시간, 모델 호출 횟수 및 일일 비용 상한을 둔다.

## 7. 웹 인터페이스 원칙

- 첫 화면에는 사이트 이름, 한 줄 소개와 뉴스 갤러리를 둔다.
- 카드에는 대표 이미지, 날짜, 짧은 제목, 한 줄 요약만 표시한다.
- 상세 화면은 콘텐츠 규격 네 영역과 이전 화면으로 돌아갈 최소 탐색만 제공한다.
- 화면 크기에 따라 한 열에서 여러 열로 자연스럽게 확장한다.
- 색상, 글꼴, 여백은 소수의 디자인 토큰으로 관리한다.
- 장식보다 읽기 편한 행 길이, 글자 크기, 대비와 로딩 성능을 우선한다.
- 이미지가 없거나 실패해도 레이아웃이 깨지지 않는 대체 상태를 제공한다.

## 8. 코드 작성 규칙

- TypeScript에서는 불가피한 경우가 아니면 `any`를 사용하지 않는다.
- 외부 입력, 환경 변수와 AI 구조화 출력은 런타임에서 검증한다.
- 함수와 모듈은 하나의 명확한 책임을 갖도록 작게 유지한다.
- 소스별 수집 로직과 공통 정규화 로직을 분리한다.
- 날짜와 시간은 저장 시 명시적인 시간대를 사용하고 화면에서 한국 표준시로 표시한다.
- 오류를 삼키지 말고 단계, 소스와 실행 식별자를 포함해 기록한다.
- 사용자에게 보이는 문구와 운영 로그를 구분한다.
- 새 의존성을 추가할 때는 필요성, 유지 상태, 번들 및 운영 비용을 검토한다.
- 구현과 문서가 달라지면 같은 변경에서 `README.md`를 갱신한다.

## 9. 데이터베이스 규칙

- 데이터베이스는 기존 Supabase 프로젝트의 새 PostgreSQL 테이블을 사용한다. Firebase 코드는 마이그레이션 완료 전 참고용으로 보존하지만 새 기능의 기본 저장소로 확장하지 않는다.
- 모든 스키마 변경은 `supabase/migrations/**`의 순서가 있는 SQL 마이그레이션으로 버전 관리한다. 적용된 마이그레이션은 임의로 수정하지 않고 새 파일로 변경한다.
- KST 발행 날짜와 slug에는 데이터베이스 고유 제약을 두고, 발행 리비전·출처 관계·공개 projection 전환을 하나의 트랜잭션에서 처리한다.
- 발행 리비전과 모델 호출 감사 기록은 불변으로 추가하며 기존 이력을 덮어쓰거나 삭제하지 않는다.
- 외래키, `check` 제약, 트랜잭션 함수와 애플리케이션 런타임 계약을 함께 사용한다. 어느 한쪽만 신뢰하지 않는다.
- 원문 URL 정규화 결과, 콘텐츠 지문, published 목록 정렬, 실행일과 lease 조회에는 필요한 고유·복합 인덱스를 둔다.
- 수집 데이터, 근거, 생성 게시물, 공개 projection과 실행 감사 데이터를 테이블·스키마 경계로 분리한다.
- Data API에 노출되는 모든 테이블·뷰는 RLS와 최소 `grant`를 적용한다. 익명 사용자는 검증된 `published` projection만 읽을 수 있다.
- Supabase Publishable Key는 공개 조회에만 사용한다. Secret Key나 직접 DB 자격 증명은 서버 전용 환경 변수에 두고 `NEXT_PUBLIC_` 접두사를 금지한다.
- lease 만료와 갱신은 클라이언트 시각이 아니라 PostgreSQL 서버 시각, 행 잠금, fencing token과 revision CAS로 판단한다.
- 운영 데이터가 필요한 테스트는 로컬 Supabase/PostgreSQL과 익명화한 픽스처를 사용하며 운영 프로젝트에 샘플 데이터를 넣지 않는다.

## 10. 보안과 개인정보

- `.env*`, API 키, 토큰과 개인 자격 증명을 커밋하지 않는다.
- 서버 전용 비밀정보를 `NEXT_PUBLIC_` 환경 변수에 넣지 않는다.
- 외부 HTML은 신뢰하지 않고 저장과 출력 전에 필요한 필드만 추출·정제한다.
- 게시물에 허용되지 않은 HTML이 들어가지 않게 한다.
- 공개 서비스에 필요하지 않은 독자 개인정보를 수집하지 않는다.
- 관리자 기능을 추가할 경우 인증과 권한 검사를 서버에서 수행한다.

## 11. 테스트와 검증

변경 범위에 비례해 다음을 검증한다.

- 단위 테스트: URL 정규화, 날짜 처리, 점수 계산, 콘텐츠 스키마
- 통합 테스트: 수집 → 정규화 → 중복 제거 → 저장 흐름
- 파이프라인 테스트: 재시도, 멱등성, 품질 실패, 발행 보류
- UI 테스트: 갤러리, 상세 화면, 빈 상태, 이미지 실패 상태
- 접근성 검사: 키보드 탐색, 제목 구조, 대체 텍스트, 대비
- 반응형 검사: 작은 모바일 화면과 일반 데스크톱 화면

완료 보고 전에는 관련 린트, 타입 검사와 테스트를 실행한다. 실행할 수 없는 검증이 있으면 생략 사실과 이유를 명확히 남긴다.

## 12. 완료의 정의

작업은 다음 조건을 충족할 때 완료된다.

- 요청된 동작이 실제로 구현되어 있다.
- 정상 흐름뿐 아니라 빈 데이터와 예상 가능한 실패도 처리한다.
- 관련 테스트 또는 검증 결과가 있다.
- 비밀정보, 저작권, 출처 및 접근성 원칙을 위반하지 않는다.
- 필요한 문서와 환경 변수 예시가 최신 상태다.
- 관련 없는 파일이나 사용자의 기존 변경을 훼손하지 않았다.

## 13. 초기 범위에서 제외하는 것

명시적인 요청이 있기 전에는 다음 기능을 구현하지 않는다.

- 회원가입과 사용자 프로필
- 댓글, 좋아요, 개인화 추천
- 복잡한 관리자 대시보드
- 실시간 뉴스 피드와 하루 여러 건 발행
- 출처가 불분명한 기사 이미지 재사용
- 품질 검사를 우회하는 무조건 자동 발행

## 14. 서브 에이전트 개발 운영 계획

이 프로젝트는 백엔드·데이터, AI·콘텐츠, 프런트엔드 작업을 서브 에이전트에게 분리하고 루트 에이전트가 계약, 자동화, 통합과 최종 검증을 책임지는 방식으로 개발한다. 단순히 역할 수만큼 에이전트를 동시에 실행하지 않고, 입력과 출력이 명확하고 파일 소유권이 겹치지 않는 작업만 병렬화한다.

### 14.1 기본 운영 구조

현재 실행 환경은 루트 에이전트를 포함해 동시 작업 슬롯이 4개이므로 한 번에 최대 3개의 서브 에이전트를 운용한다. 실행 환경의 슬롯 수가 달라져도 루트 에이전트용 슬롯 하나는 항상 남긴다.

| 담당 | 기준 프로필 | 핵심 책임 |
|---|---|---|
| 루트 에이전트 | 최민재 | 계획, 공통 설정, 공유 계약, 오케스트레이션, CI/CD, 통합과 최종 검증 |
| 백엔드 서브 에이전트 | 김도윤 | DB, 수집기, 정규화, 중복 제거, 저장소 |
| AI·콘텐츠 서브 에이전트 | 박서연 | 후보 선정, 구조화 생성, 근거·품질 검사, 발행 차단 |
| 프런트엔드 서브 에이전트 | 이현우 | 갤러리, 상세 화면, 반응형 UI, 접근성 |

- 루트 에이전트는 조율만 기다리지 않고 공통 기반과 통합 작업을 계속 진행한다.
- 서브 에이전트는 루트가 지정한 하나의 구체적이고 검증 가능한 작업만 수행한다.
- 작은 문서 수정, 하나의 파일에 집중된 변경, 계약이 아직 정해지지 않은 작업은 루트가 직접 수행한다.
- 서브 에이전트는 명시적인 허가 없이 다시 하위 에이전트를 만들지 않는다.
- 외부 게시, 운영 배포, 유료 API 호출과 자격 증명 변경은 서브 에이전트에게 위임하지 않는다.

### 14.2 고정 파일 소유권

동시에 같은 파일을 수정하지 않도록 기본 소유권을 정한다. 실제 디렉터리가 생성되기 전에도 이 경계를 기준으로 작업을 배정한다.

#### 루트 에이전트

- `package.json`, 패키지 잠금 파일
- `tsconfig.json`, Next.js·린트·테스트 공통 설정
- `.env.example`
- `src/contracts/**`
- `src/lib/config/**`, `src/lib/logger/**`
- `src/pipeline/orchestrator/**`
- `src/app/api/cron/**`
- `.github/workflows/**`, `vercel.json`
- `scripts/**`
- `tests/integration/**`, `tests/e2e/**`
- `README.md`, `AGENTS.md`

#### 백엔드·데이터 서브 에이전트

- `src/db/**`
- `src/repositories/**`
- `src/pipeline/collectors/**`
- `src/pipeline/normalize/**`
- `src/pipeline/deduplicate/**`
- `supabase/migrations/**`, Supabase 데이터 접근 구현과 로컬 검증 설정
- 기존 `firestore.rules`, `firestore.indexes.json`, `firebase.json`은 제거 결정 전까지 참고용으로 보존
- `tests/backend/**`

#### AI·콘텐츠 서브 에이전트

- `src/pipeline/scoring/**`
- `src/pipeline/retrieval/**`
- `src/pipeline/generation/**`
- `src/pipeline/quality/**`
- `src/prompts/**`
- `tests/content/**`
- `tests/fixtures/content/**`

#### 프런트엔드 서브 에이전트

- `src/app/**` 중 `src/app/api/cron/**` 제외
- `src/components/**`
- `src/styles/**`
- `public/**`
- `tests/ui/**`

공유 계약, 공통 설정, 패키지 의존성은 루트만 수정한다. 다른 담당자의 파일 변경이 필요하면 직접 수정하지 말고 계약 변경 요청과 이유를 루트에 전달한다. Supabase SQL 마이그레이션, RLS와 인덱스는 백엔드 담당 한 명이 제안하고 루트가 적용 순서와 호환성을 검증한다.

### 14.3 계약 우선 원칙

병렬 개발을 시작하기 전에 루트가 `src/contracts/**`에 최소한 다음 계약을 정의한다.

- `ArticleInput`
- `NormalizedArticle`
- `TopicCandidate`
- `GeneratedPost`
- `QualityResult`
- `PublishedPost`
- `PipelineRunState`
- 갤러리 목록과 게시물 상세 조회 형태
- 파이프라인 단계 이름과 공통 오류 형태

서브 에이전트는 공유 타입을 복제하거나 임의의 유사 타입을 만들지 않는다. 계약이 부족하면 작업을 멈출 필요 없이 가능한 독립 부분을 진행하고, 필요한 필드와 사용 사례를 루트에 요청한다. 루트는 변경 영향 범위를 확인한 뒤 계약을 한 번 수정하고 모든 담당자에게 알린다.

외부 입력, AI 출력과 API 응답 계약은 TypeScript 타입뿐 아니라 런타임에서도 검증한다. 날짜·시간은 시간대를 포함한 ISO 8601 값을 기본으로 사용하고, 한국 표준시의 발행 기준일 `YYYY-MM-DD`와 순간을 나타내는 타임스탬프를 구분한다. 필수·선택·`null` 허용 여부와 빈 배열의 의미도 계약에 명시한다.

### 14.4 단계별 개발 순서

#### 단계 0 — 공통 기반 확정: 루트 순차 작업

루트가 실행 가능한 Next.js·TypeScript 프로젝트, 린트·타입 검사·테스트 명령, 환경 변수 검증 골격과 공유 계약을 만든다.

완료 기준:

- 빈 프로젝트가 빌드된다.
- 린트와 타입 검사를 실행할 수 있다.
- 세 서브 에이전트가 공유 계약만으로 독립 작업을 시작할 수 있다.

#### 단계 1 — 기능 골격: 최대 3개 병렬 작업

백엔드 서브 에이전트:

- Supabase PostgreSQL 테이블·함수, RLS와 복합 인덱스 작성
- 날짜별 게시물 고유 제약과 중복 탐지용 인덱스 추가
- 공개 게시물 조회 저장소와 테스트 구현

AI·콘텐츠 서브 에이전트:

- 구조화 생성 결과 스키마와 가짜 생성 공급자 구현
- 결정론적인 후보 점수 계산 구현
- 네 영역의 콘텐츠 형식과 출처를 검사하는 품질 게이트 구현

프런트엔드 서브 에이전트:

- 고정 픽스처를 사용하는 카드 갤러리와 상세 화면 구현
- 빈 상태, 이미지 실패 상태, 모바일·데스크톱 레이아웃 구현
- 시맨틱 HTML과 키보드 탐색 검증

루트 에이전트:

- 구조화 로그, 환경 변수와 파이프라인 단계 인터페이스 구현
- 에이전트 질문과 계약 변경 요청 조정

완료 기준:

- 각 영역이 실제 외부 API 없이 독립적으로 테스트된다.
- 공유 계약과 다른 임의 타입을 사용하지 않는다.
- `published` 상태가 아닌 글은 공개 조회에 나타나지 않는다.

#### 단계 2 — 실제 기능 연결: 의존성별 병렬 작업

백엔드 서브 에이전트:

- 허용 범위를 확인한 RSS 또는 공식 API 소스 1~2개 연결
- 제한 시간, 호출 속도, URL 정규화, 콘텐츠 지문과 중복 방지 구현
- 한 소스의 실패를 격리하는 수집 통합 테스트 작성

AI·콘텐츠 서브 에이전트:

- 실제 LLM 공급자를 공급자 인터페이스 뒤에 연결
- 구조화 출력 런타임 검증과 근거 기반 생성 구현
- 출처·중복·중립성·가독성 검사와 비용 한도 차단 구현
- 모델, 프롬프트 버전, 비용과 출처 감사 데이터 생성

프런트엔드 서브 에이전트:

- 픽스처를 실제 읽기 저장소로 교체
- 최신순 목록, 상세 메타데이터, KST 날짜와 이미지 최적화 구현

루트 에이전트:

- `collect → normalize → deduplicate → score → generate → validate → publish` 오케스트레이터 구현
- 제한 재시도, 지수 백오프, 완료 단계 재사용과 실행 한도 구현
- Cron 인증과 마지막 단계에서만 `published`로 전환하는 흐름 구현

완료 기준:

- 테스트에서는 외부 API가 모킹된다.
- 같은 날짜로 두 번 실행해도 게시물은 최대 한 건이다.
- 생성이나 품질 검사 실패 시 초안이 공개되지 않는다.
- 일부 수집기가 실패해도 사용 가능한 후보 처리를 계속한다.

#### 단계 3 — 전체 통합: 루트 순차 작업

이 단계에서는 새 기능을 병렬로 추가하지 않는다. 루트는 다음 순서로 통합한다.

1. 파일 소유권 위반과 계약 차이를 확인한다.
2. Supabase 마이그레이션, RLS, 함수 권한과 복합 인덱스를 검토한다.
3. 린트, 타입 검사와 전체 단위 테스트를 실행한다.
4. 고정 픽스처로 일일 파이프라인을 끝까지 실행한다.
5. 생성된 글이 공개 갤러리와 상세 화면에 정확히 나타나는지 확인한다.
6. 실패 지점을 해당 소유 에이전트에게 작은 수정 단위로 재배정한다.
7. 수정 후 전체 검증을 처음부터 다시 실행한다.

완료 기준:

- 하나의 실행 ID로 모든 단계 상태를 추적할 수 있다.
- 게시물과 참고 출처의 관계가 보존된다.
- 품질 검사를 통과한 글만 공개된다.
- 재실행해도 기사, 출처 관계와 게시물이 중복되지 않는다.

#### 단계 4 — 영역별 강화: 최대 3개 병렬 작업

- 백엔드: 잘못된 날짜, 중복 URL, 타임아웃, 부분 소스 실패, 트랜잭션을 검증한다.
- AI·콘텐츠: 근거 없는 사실, 광고성 표현, 유사 주제, 출처 누락과 발행 보류를 회귀 테스트한다.
- 프런트엔드: 모바일·데스크톱 시각 상태, 접근성, 긴 제목, 빈 갤러리와 이미지 실패를 검증한다.
- 루트: CI, 스케줄, 실패 알림, 비밀정보 검사, 백업·복구 절차와 운영 문서를 완성한다.

완료 기준:

- 린트, 타입 검사, 단위·통합·UI 테스트가 통과한다.
- 실패 알림에 실행 ID, 단계와 원인이 포함된다.
- API 키가 코드, 클라이언트 번들과 로그에 나타나지 않는다.
- 재실행과 복구 방법이 운영 문서에 기록된다.

#### 단계 5 — 실제 배포: 승인 후 루트 순차 작업

운영 데이터베이스, 자격 증명, 유료 API 또는 외부 게시가 필요하므로 사용자 승인과 환경 준비 후 진행한다.

1. 기존 Supabase 프로젝트에 승인된 마이그레이션 적용, RLS·함수 권한과 빈 상태 검증
2. 환경 변수 등록과 프리뷰 배포
3. Cron 수동 시험과 실패 알림 시험
4. 프로덕션 전환
5. 다음 한국 표준시 예약 실행 결과 확인

완료 기준:

- 실제 예약 작업이 하루 한 번 실행된다.
- 중복 실행에서도 하루 최대 한 건 제약이 유지된다.
- 실패 시 미완성 글이 공개되지 않고 운영 알림이 전달된다.

### 14.5 서브 에이전트 작업 요청 형식

루트는 서브 에이전트를 만들 때 아래 정보를 빠짐없이 전달한다.

```text
작업 ID와 담당 역할:
목표:
필수 입력 파일:
선행 조건과 사용 가능한 계약:
수정 허용 경로:
수정 금지 경로:
구현 요구사항:
반드시 처리할 실패 상황:
실행할 검증 명령:
완료 보고 형식:
```

서브 에이전트의 완료 보고에는 다음이 포함되어야 한다.

- 변경 파일 목록
- 구현한 동작과 중요한 결정
- 실행한 검사와 결과
- 생략한 검사와 이유
- 남은 위험 또는 계약 변경 요청
- 다른 작업이 사용할 공개 인터페이스

### 14.6 병렬 작업과 통합 규칙

- 루트는 작업 시작 전에 현재 변경 상태와 각 에이전트의 허용 경로가 겹치지 않는지 확인한다.
- 같은 파일이나 Supabase 마이그레이션·RLS·함수에 두 에이전트를 동시에 배정하지 않는다.
- 서브 에이전트는 요청 범위를 벗어난 리팩터링, 패키지 설치와 공통 설정 변경을 하지 않는다.
- 공유 작업공간의 다른 변경을 되돌리거나 덮어쓰지 않는다.
- 서브 에이전트는 별도 지시 없이 커밋, 푸시, 외부 게시 또는 배포하지 않는다.
- 저장소 전체 포매팅, 자동 코드 생성과 일괄 이름 변경은 루트가 독점 작업으로만 실행한다.
- 실제 외부 API 대신 고정 픽스처와 가짜 공급자로 재현 가능한 테스트를 먼저 만든다.
- 실제 뉴스 수집, LLM 호출과 배포는 별도의 통합 단계에서 연결한다.
- 완료 순서대로 자동 통합하지 않고 루트가 계약, 변경 범위와 테스트 결과를 검토한다.
- 중간 산출물이 준비되면 모든 에이전트를 기다리지 않고 즉시 검토하되, 의존 계약이 확정되기 전에는 통합하지 않는다.
- 중간 또는 높은 위험의 변경은 구현하지 않은 다른 에이전트가 읽기 전용으로 검토하고 루트가 수정 여부를 판단한다.
- 통합 실패는 원인을 소유한 에이전트에게 재현 방법과 허용 경로를 포함한 작은 수정 작업으로 재배정한다.
- 같은 차단 원인이 반복되면 범위를 더 작게 나누거나 루트가 직접 인수한다.
- 최종 린트, 타입 검사, 전체 테스트와 배포 판단은 루트 에이전트가 책임진다.

### 14.7 검토 게이트

모든 구현은 아래 게이트를 순서대로 통과한다.

1. **계약 게이트**: 데이터 모델, API, 상태와 오류 의미가 구현 전에 정해져 있다.
2. **영역 게이트**: 담당 기능과 경계 조건 테스트가 통과한다.
3. **교차 검토 게이트**: 작성자가 아닌 에이전트가 계약 위반, 보안과 데이터 손실 위험을 읽기 전용으로 검토한다.
4. **통합 게이트**: 전체 린트, 타입 검사, 테스트와 빌드가 통과한다.
5. **발행 안전 게이트**: 한국 표준시 하루 최대 한 건, 검증 전 비공개, 출처 보존, 안전한 재실행과 비밀정보 미노출을 확인한다.

공통 계약, Supabase 마이그레이션·RLS·함수 권한, 발행 상태 전이와 예약 작업은 반드시 작성자 외 한 명 이상의 검토를 받는다. 검토자는 발견 사항을 먼저 보고하고, 루트가 별도의 수정 작업을 배정하기 전에는 작성자의 파일을 편집하지 않는다.

### 14.8 차단과 재배정

- 서브 에이전트는 같은 원인의 해결 시도가 두 번 실패하면 무한 반복하지 않고 차단 상태를 보고한다.
- 차단 보고에는 실패 명령, 핵심 오류, 시도한 해결, 현재 변경 파일과 다음 제안을 포함한다.
- 범위가 예상보다 커지거나 계약 변경이 필요하면 허용 경로 밖을 수정하기 전에 보고한다.
- 부분 구현을 숨기거나 임의로 되돌리지 않고 통과·실패한 검증과 함께 인계한다.
- 루트는 재배정 전에 기존 담당자의 실행을 중단하고 공유 작업공간의 변경 상태를 확인한다.
- 새 담당자에게 기존 실패 원인, 이미 시도한 접근, 변경된 계약과 우선 확인할 파일을 함께 전달한다.
- 같은 쓰기 범위에는 기존 담당자와 새 담당자를 동시에 배정하지 않는다.
- 원인이 공통 계약이나 기반 설정이면 개별 소비자를 각각 우회 수정하지 않고 루트가 공통 원인을 먼저 해결한다.

차단 또는 부분 완료 인계에는 다음 형식을 사용한다.

```text
작업 ID:
상태: 완료 | 부분 완료 | 차단
구현 요약:
수정 파일:
계약 또는 Supabase 스키마·RLS·함수 변경:
실행한 명령과 결과:
실행하지 못한 검증과 이유:
차단 원인과 시도한 해결:
알려진 위험과 남은 작업:
후속 담당자가 먼저 확인할 사항:
```

### 14.9 README 단일 기록 책임

사용자가 요청한 대로 개발 과정에서 변경되거나 수정되는 모든 통합 작업의 결과는 `README.md`에 기록한다. 충돌과 중복을 피하기 위해 README의 유일한 쓰기 소유자는 **최민재 프로필을 맡는 루트 에이전트**다.

- 서브 에이전트는 `README.md`를 직접 수정하지 않는다.
- 모든 서브 에이전트는 완료·부분 완료·차단 보고에 `README 반영 항목`을 포함한다.
- 루트는 서브 에이전트 결과를 검토·통합한 것과 같은 변경에서 README를 갱신한다.
- README에는 현재 구현 상태, 확정·변경된 제품 및 기술 결정, 사용자에게 보이는 동작, 계약·스키마·환경 변수, 검증 결과, 알려진 제한과 다음 작업을 반영한다.
- `README.md`, `AGENTS.md`, `coder/*.md` 자체가 변경되어도 변경 기록 표에 남긴다.
- 코드 줄별 차이는 Git이 보존한다. README에는 사람이 이해하고 다음 개발을 이어갈 수 있는 작업 단위의 이유와 결과를 빠짐없이 기록한다.
- README 변경 기록은 `날짜 / 작업 ID / 기록자 / 참여 프로필 / 변경 요약 / 영향 파일 / 검증 / 남은 결정` 형식을 따른다.
- 문서 반영이 필요한 작업에서 README 갱신이 끝나지 않으면 완료로 처리하지 않는다.

루트는 최종 보고 전에 다음을 확인한다.

1. README의 제품 설명과 현재 구현이 일치하는가
2. 기술·데이터·공개 계약 변경이 해당 섹션에 반영되었는가
3. 환경 변수와 실행·검증 방법이 최신인가
4. `현재 상태`와 다음 작업이 갱신되었는가
5. 변경 기록에 작업 ID와 검증 결과가 추가되었는가

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
